# Results Guidelines

- Add or update preserved results only when the user explicitly requests it. Do not automatically collect experiment outputs here.
- Use a separate folder for each result, usually with a `YYYYMMDD` date suffix, and include a `README.md` with its source run.
- Use symbolic links for large files. HTML presentations are welcome.
