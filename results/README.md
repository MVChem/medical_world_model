# Results

Important experiment results selected by the user, usually collected from `runs/` for presentation, including HTML pages.

- Use one folder per result, usually named `name_YYYYMMDD/`.
- Include a `README.md` describing the contents and source run in each folder.
- Use symbolic links for large files.

Only preserve results here at the user's explicit request. See [AGENTS.md](AGENTS.md).
