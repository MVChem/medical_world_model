# 2026-09-14 最终权重保留规则

用户已确定：正式实验选最终权重。以下规则替代初次空间审计中同时保留独立 best 的建议。

**执行完成：已按用户授权删除清单中的 545 个权重文件，释放 32.80 GiB。** 清理前后 15 份表格训练权重、26 个原始指标来源、共享 TeX 表格和 PDF 的 SHA256 均一致；保留权重和 135 个图像／特征缓存文件也通过核验。Qwen9B 与 SwinIR 的训练仍在继续。

- 正式实验保留 `checkpoint_final.pt`；使用覆盖保存方式的已完成实验保留其最终 `checkpoint.pt`。
- 有最终权重替代时，独立 best、epoch 快照及中途／early 权重列入清理候选；保留原有配置、代码、日志和评估记录。
- 保留正式实验需要的 Stage 1／共享初始化、预训练基座及评分模型。
- 正在训练或排队任务的文件整体保留，包括恢复训练所需的最新检查点。
- 某些旧实验只有名为 `best_regressor.pt`／`best_decoder.pt` 的成品，没有 final 替代，继续保留，不能只凭文件名删除。
- 图像、特征、伪标签缓存不在此次权重候选清单内。

## 已执行的清理范围

| 类别 | 文件数 | 实际占用 |
| --- | ---: | ---: |
| 已结束的试跑、重构验证、显存测试等权重 | 130 | 24.19 GiB |
| 已有 final 的旧 latest | 12 | 2.24 GiB |
| 额外 epoch 快照 | 369 | 0.58 GiB |
| 有 final 替代的独立 best | 21 | 3.21 GiB |
| 历史中途／early 权重 | 13 | 2.59 GiB |
| **合计** | **545** | **32.80 GiB** |

相较初次审计的 504 个／26.99 GiB，新增 41 个／5.81 GiB。统计按实际分配的磁盘块、独立路径计算；各类互不重叠，重复内容验证的 5.67 GiB 不额外相加。

当前 Table 1／Table 2 已有数值使用的最终权重保留，指标及来源没有变动。历史中途评估记录继续保留，其权重已按本规则清理；再精确重跑那些中途评估需重新生成对应权重。

实际删除及核验结果见 [执行记录](../results/storage_audit_20260914/cleanup_execution.json)，表格权重与依赖的保护证据见 [保护记录](../results/storage_audit_20260914/table_weight_protection.json)。[逐文件清单](../results/storage_audit_20260914/weights_final_only_plan.json) 的状态已更新为 `complete`。未修改正在运行任务的保存代码。初次审计仍保留在 [原始记录](0914_storage_weight_audit.md) 中。
