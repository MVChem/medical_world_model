# Install these optional official report-evaluation dependencies without changing
# the training environment's torch/transformers pins. See the preflight probe.
# The isolated CPU worker additionally uses an asset-local --target installation
# of transformers==4.51.3, tokenizers==0.21.4, huggingface-hub==0.34.4.
# Never install those worker pins into the Qwen training environment.
radgraph==0.1.18
appdirs==1.4.4
dotmap==1.3.30
jsonpickle==4.1.2
nltk==3.10.3
h5py==3.16.0
filelock==3.29.1
