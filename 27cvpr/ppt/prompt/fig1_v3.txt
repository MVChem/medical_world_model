Create a publication-ready Figure 1 for MedWorld-JEPA. The figure shows how medical image tokens and text tokens enter a shared VLM, whose patient-state tokens support downstream tasks and a latent world model. Real future observations provide a prediction loss that trains the representation of the current patient. Generate the entire figure from this text description.

VISUAL STYLE
Use a compact landscape canvas, approximately 2:1 in width to height, with a pure white background. Draw a clean, colorful neural-network schematic with flat fills, clearly outlined modules, small token glyphs, and sparse labels. Use an open layout with no outer frame, decorative background, drop shadows, or three-dimensional effects. Small grayscale medical-image thumbnails are the only photographic elements.

Use distinctive geometric shapes for different module types. Draw the image encoder as a cyan-blue trapezoid with a tall left edge and a shorter right edge, tapering toward its output. Draw the VLM, task decoder, and world model as larger rounded rectangular containers. Draw lightweight output heads as slim pale-blue trapezoids that widen toward the right, with their short names written vertically. Draw resampling and projection modules as small, plain rectangular blocks.

Use this palette: cyan-blue #9BCAE9 for the image encoder, with a stronger blue outline; mauve-pink #D399D9 for the VLM container, with a dark plum outline #68256C; light green #B8E094 for the task decoder, with a darker olive-green outline #557C2F; warm beige #D8CBBB for the world-model container, with a brown outline #8C6639; pale cream-yellow #FFFFCD for internal Transformer blocks; and very pale blue #DFE9F6 for output heads. Use cyan, pink, and green tokens to match the modules they belong to. Keep text and principal arrows black. Secondary feature connections are light gray. Future target tokens are neutral gray, without a colored background panel.

Inside the large neural-network containers, show a few tall, narrow, pale-yellow rectangular blocks with black outlines, arranged side by side. Place an ellipsis between representative blocks to suggest repetition without specifying a layer count. Use compact vertical labels such as "Transformer block" or "Predictor block". Place each container's name clearly along its top edge. The internal blocks should be a simple visual shorthand, occupying little space and leaving clear room for incoming and outgoing arrows.

Draw tokens as tiny rounded squares with even gaps. Use short horizontal strips for VLM input sequences and vertical columns at encoder or decoder outputs. Place mathematical labels such as D_t, S_t, and Y_hat beside the corresponding token group in bold italic mathematical lettering. Use a compact, rounded, slightly handwritten sans-serif style similar to Comic Sans for module titles, and a clean narrow sans-serif for small internal labels. Keep all lettering dark and readable. Use consistent medium-thin outlines and black arrows with small solid triangular heads. Route connections horizontally or vertically with clean right-angle bends. Use lighter gray routing for secondary connections. Keep arrows clear of modules and labels. Small orange flame symbols at the task-decoder and predictor corners may indicate trainable modules; do not put snowflakes or frozen labels on the online encoder, VLM adapters, or task decoder.

LAYOUT
Read the figure from left to right. Place compact image and text evidence on the left, the V-JEPA encoder and resampler just to their right, and the shared VLM at the center. Place a clearly visible column of patient-state tokens immediately after the VLM. Split this same token column into two branches on the right: an upper task-decoding branch and a lower world-model branch. Align the branches horizontally with their own outputs at the far right. Use short module names, ample space between branches, and no large explanatory text boxes, figure caption, title banner, or color legend.

CURRENT EVIDENCE AND VLM INPUT
Show a current medical scan thumbnail and a small optional prior-study thumbnail, labeled "Current / prior studies". Radiographs, CT slices, and MRI slices are examples of available imaging, not a requirement that every patient has all modalities. All online evidence is available at or before time t.

Images flow into one trapezoidal "V-JEPA encoder". Its output is a single column of cyan image-feature tokens labeled D_t. These pass through "Resampler + projector" to form compact visual tokens. Show only one feature resolution.

Below the image input, place a small text card labeled "Report / history / timing". A small "Tokenizer + embedding" block converts this text into neutral-colored text tokens. Text does not pass through the image encoder.

Bring visual tokens and text tokens together into one ordered strip entering the VLM:
visual tokens | text tokens | learned state slots
Draw eight generic learned state-slot glyphs at the end of this strip. Show this input sequence only once. It must be clear that image and text tokens enter the same VLM.

SHARED PATIENT STATE
Label the central pink container "VLM" with a small "LoRA" annotation. The contextualized hidden states at the eight learned slot positions become the output patient-state tokens S_t. Draw these as a short vertical column of pink tokens, visually separate from the input slots, and label them "Patient-state tokens". Add the small annotation "Shared current state". These are continuous hidden representations, not generated words. Both output branches read the same S_t; do not assign permanent task roles to individual slots. Construct S_t before any task query or forecast horizon is supplied.

UPPER BRANCH: DOWNSTREAM TASKS
Route S_t into a light-green "Task decoder" container. A small separate "Task query" token also enters this decoder. Its output tokens enter a pale-blue tapered "Task head". Show two compact example outputs labeled "Clinical prediction" and "Segmentation": a simple score glyph and a small medical mask thumbnail. A thin, light-gray connection from the single-level image features D_t supplies spatial detail directly to the segmentation decoder/head. Keep this connection secondary to the S_t route; it is a single-level spatial feature connection. The query decoder reads the cached state without writing new values back into it. Task supervision can still train the VLM adapters, learned slots, resampler, and trainable encoder through backpropagation. Put a small "Task loss" label near the task outputs, without a long feedback loop.

LOWER BRANCH: FUTURE PREDICTION
Route the same S_t into the warm-beige "World model" container, with the subtitle "Latent predictor". Introduce small conditioning inputs labeled "Forecast query" and "Horizon h" only at this branch. Group the transition-query readout and latent predictor within this one container; do not expand a separate A_t,h module. A thin, light-gray connection from D_t also supplies current visual context to the world model. The predictor emits a single column of "Predicted future tokens". These represent future image embeddings, not a generated future medical image. Show no separate global or regional prediction heads.

COMPACT FUTURE SUPERVISION
Beside the predicted-token column, place one small matching column of gray "Future target tokens". Under it, write "Real follow-up; EMA, stop-grad". Connect the predicted and target columns to a small "Prediction loss" node. This local cue conveys the future supervision; do not draw another image encoder, an EMA update formula, a full-width lower pathway, or a green training-only panel. Future target tokens never enter the current VLM or the predictor as forward inputs. Add one short note near the loss: "Future loss shapes current-state tokens". The future loss trains the online predictor and state-producing path; only the target side is stop-gradient.

ARCHITECTURAL CONSTRAINTS
Use one V-JEPA encoder output level, one resampling stage, one shared VLM, one set of patient-state tokens, and two readout branches. Remove all multiscale elements: no feature pyramids, no 1/4-1/8-1/16 labels, no D_t^{1:L}, no global/regional target hierarchy, and no multiscale loss. Do not add gaze or gesture modules, body/head masks, Q/K/V fusion diagrams, robot actions, intervention semantics, or other tasks. Make the shared image-text representation and its two uses immediately readable.
