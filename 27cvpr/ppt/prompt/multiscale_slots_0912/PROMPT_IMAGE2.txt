Create a compact, publication-quality methods schematic for a medical multimodal representation paper. The figure explains how eight state slots are constructed from four depths of a vision-language model and four depths of a visual encoder, then jointly trained with a downstream task decoder. Use a white background, a landscape canvas approximately 2:1, flat vector-style geometry, thin consistent lines, readable sans-serif labels, and generous whitespace. It must remain legible as a small methods panel. All visible text is English.

Organize a single left-to-right diagram: two feature-source lanes on the left, eight state slots at the center, and task decoding plus supervision on the right.

UPPER LANE — FUSED SEMANTIC FEATURES
Label the source “VLM: vision–language fusion”. Both visual tokens and available report/context tokens enter this module. Show a depth stack with exactly FOUR highlighted hidden-state taps, ordered “Early”, “Middle 1”, “Middle 2”, “Late”. Distribute them approximately uniformly across the full depth, with one early tap, two middle taps, and one late tap. Faint intervening blocks or ellipses indicate omitted layers. Each selected layer passes through a small trainable slot-readout symbol and yields exactly ONE plum-colored slot. Label the four output slots “S1”, “S2”, “S3”, “S4”, and their group “4 semantic slots”. Keep the one-layer-to-one-slot mapping explicit.

LOWER LANE — VISUAL FEATURES
Label the source “Visual encoder (V-JEPA)”, with a small “Frozen” label. A simple task-image icon labeled “Image X” enters it. Mirror the upper lane with exactly FOUR highlighted taps: “Early”, “Middle 1”, “Middle 2”, “Late”, approximately uniformly spaced across its own full depth. Each selected visual layer passes through a trainable slot-readout symbol and yields exactly ONE teal-colored slot. Label these “S5”, “S6”, “S7”, “S8”, with the group label “4 visual slots”. Add a slim connection from the visual encoder’s final output through “Visual adapter” into the VLM’s visual input. These lanes belong to one connected encoding pipeline.

CENTRAL STATE
Arrange the eight output slots as two aligned rows of four: plum S1–S4 above teal S5–S8. Enclose them in one subtle frame labeled “8 state slots · S ∈ R^(8×d)”. Use the same small token shape and size for all eight. Add the short annotation “Multi-depth semantic + visual information”. Depth does not imply different image resolutions. The slots are vectors, not image patches. The small readout symbols summarize trainable aggregation/projection into one d-dimensional slot per selected layer; do not specify an unprovided attention or pooling architecture.

RIGHT — TASK SUPERVISION
Connect the state through a clean arrow labeled “Task readout” to one box “Task decoder Dq”, then to “Prediction ŷ”, then to “Task loss Lq”. A compact “Target y” enters only the loss. An optional “Task query Qq” enters only the decoder. One fine gray path along the lower edge connects Image X to the decoder, labeled “Image input for spatial tasks”; for super-resolution, X is the low-resolution input throughout. Keep the decoder generic to represent task-dependent slot selection.

Show a warm coral DASHED return path from the task loss, labeled “Joint optimization”, with arrowheads reaching the decoder, trainable slot readouts, and trainable VLM/adapter components. The frozen visual encoder is not updated. Solid arrows indicate forward computation; dashed arrows indicate training gradients. Slots depend on the input; they are not independently optimized patient-specific parameters.

Keep the hierarchy simple: the eight slots and their four-plus-four origins are the visual focus. Use muted plum for fused semantics, muted teal for visual features, pale amber for the decoder, coral for gradients, and dark gray text. Avoid dense prose, decorative anatomy, performance numbers, disease-specific slot labels, extra slots, future-prediction modules, image-resolution pyramids, photorealistic scans, 3D effects, logos, and watermarks. Treat information retention as the design objective, without claims of proven improvement.
