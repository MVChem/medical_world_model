Create a publication-ready Figure 1 for a CVPR paper introducing MedWorld-JEPA. Show the main idea, training pipeline, and data flow in one clear landscape illustration: real future observations teach the model to build a better reusable representation of the patient at the present time.

Use a simple left-to-right flow. Start with multimodal evidence available at or before time t: a current medical study, optional prior 2D or 3D studies, report, history, timing, and clinical context. Images pass through a trainable medical 2D / 3D encoder. Its output splits into a multiscale dense spatial memory D_t^{1:L} for the current study and a resampler/projector that produces compact visual tokens. The report and history join these tokens as text evidence.

At the center, show a VLM with LoRA receiving the visual and text evidence followed in the same token sequence by eight generic learned patient-state slots. The returned slot states form S_t. Present D_t^{1:L} and S_t together as the reusable current-patient representation R_t. This central present-state motif should be the visual focus, formed before any task or forecast horizon is given.

Show two small read-only uses of R_t. A query decoder reads S_t for ordinary downstream tasks, such as a global clinical prediction or a dense localization/segmentation output that also uses D_t^{1:L}. The main forecasting path adds a requested horizon h only after S_t is formed: a transition query produces A_t,h, then a latent predictor combines D_t^{1:L}, S_t, A_t,h, and h to forecast one global future latent plus a few regional multiscale future latents.

Keep the observed future in a separate training-only branch. A real follow-up study X_t+ passes through an EMA target encoder under stop-gradient to produce a matching global-and-regional latent hierarchy. Bring predicted and target latents together at a small alignment-loss symbol, with a subtle learning arrow showing that the future loss shapes the online current representation. Do not send the future study into the current-evidence pathway and do not draw a generated future image.

Use the current Figure 1 as a loose style reference: wide and uncluttered, white background, small grayscale medical thumbnails, pale rounded modules, token strips, thin arrows, blue for the current-state path, orange for queries and prediction, and green for the target-only branch and loss. Keep labels short and details sparse. Let the image model decide the exact composition, typography, icons, spacing, and connector shapes. Avoid robot/action imagery or causal treatment claims.
