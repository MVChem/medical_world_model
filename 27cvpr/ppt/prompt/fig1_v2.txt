Create a clean, publication-ready Figure 1 for a CVPR paper introducing MedWorld-JEPA. Show the main idea and training data flow without turning the figure into a detailed engineering diagram: observed future scans provide latent supervision that improves a reusable representation of the patient at the present time.

Use a wide landscape composition with one main left-to-right online pathway and one simple training-only target pathway below it.

On the left, show “Current evidence at or before t”: a current medical study, optional prior 2D or 3D studies, and compact symbols for report, history, timing, and clinical context. Only the images enter a trainable “Medical 2D / 3D encoder”. Its output splits into two routes. One route preserves the current study’s multiscale dense spatial memory D_t^{1:L}. The other passes image features through “Resampler + projector” to create a small number of visual tokens. Text does not enter the medical image encoder: report, history, and context pass through the VLM’s native tokenizer and embedding layer to become text tokens.

Make the VLM input explicit and show it only once as a single ordered sequence:

visual tokens | text tokens | 8 learned slot embeddings s_1...s_8

Feed this sequence into “VLM + trainable LoRA”. The contextualized hidden states returned at the eight slot positions are the patient-state slots S_t. Clearly distinguish the learned input slots s_1...s_8 from the patient-specific output states S_t. Do not draw visual or text tokens flowing directly into R_t.

Group D_t^{1:L} and S_t with a simple brace or open grouping labeled “Reusable current representation R_t = (D_t^{1:L}, S_t)”. R_t is a conceptual bundle, not another neural-network block. Make this present-state representation the central visual focus and indicate that it is formed before any task query or forecast horizon is supplied.

From S_t, draw a small read-only downstream branch: “Task query” enters a “Read-only query decoder”, producing the task-specific latent e_t^(q), which then feeds a lightweight task head. Show one global clinical output and one dense segmentation/localization output. The query decoder reads only S_t; D_t^{1:L} bypasses it and connects directly to the dense task head.

For forecasting, introduce the requested horizon h only after S_t has been formed. S_t together with a transition query and h produces A_t,h. A compact “Latent predictor” receives exactly D_t^{1:L}, S_t, A_t,h, and h, and outputs predicted future latents: one global latent and a few regional multiscale latent grids. Do not draw a generated future medical image.

In the lower training-only pathway, show the real observed follow-up study X_{t+} passing through an “EMA target encoder · stop-gradient” and becoming a matching set of target future latents: one global latent and regional multiscale grids. Place the target and predicted latent sets close together and connect both directly to a small “Alignment loss” node. Add a short nearby note such as “future loss trains the online path”; do not draw a long feedback arrow looping back to R_t.

Keep every connector easy to trace. Use straight horizontal or vertical arrows, allowing only short clean right-angle turns when necessary. Do not use sweeping curves, loops, winding feedback paths, perimeter-following arrows, or connectors that cross modules.

Use a restrained visual style similar to the current figure: white background, generous whitespace, small grayscale medical thumbnails, pale rounded modules, simple token and feature-map glyphs, and thin dark arrows. Use blue for the current representation pathway, orange for forecasting, green for the target-only pathway and loss, and purple for patient-state slots. Keep labels short and let the image model decide the precise typography, spacing, icons, and proportions.

Do not include a legend or color key. Avoid duplicated token rows, unnecessary internal architecture, robot/action imagery, intervention or causal-treatment claims, and any future-to-current forward-data arrow.
