Draw a compact, elegant CVPR paper schematic of the multi-depth slot construction module in MedWorld-JEPA. Use one shallow horizontal composition suitable for spanning two paper columns, with no upper/lower panels.

Show only the module: VLM feature layers → lightweight readouts → eight state slots. Within the VLM block, distinguish vision–language layers in pink and native vision-encoder layers in blue. Select four depths from each source; source-specific readouts form four pink vision–language slots and four blue visual slots, labeled “8 slots” and “8 × d”. Visual slots use only pre-fusion visual features. Indicate “Visual spatial consistency” with a small training-only regularization annotation attached to the blue slots, without expanding its mechanism.

Keep the style friendly and polished: white background, pastel rounded boxes, simple arrows, slightly handwritten lettering, and pale yellow readouts. Use very few words and generous breathing room. No input images, output examples, task decoders, teacher/crop pipelines, or separate supervision panel. Freely choose the visual arrangement and details to make the module clear and beautiful. Return only the figure.
