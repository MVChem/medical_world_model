Create a beautiful, publication-quality Figure 2 for a CVPR paper on MedWorld-JEPA, explaining multi-depth state-slot construction and visual slot spatial consistency. Make it a compact, wide landscape figure spanning both paper columns, with a modest height, approximately 3:1 in aspect ratio. Keep labels readable at full two-column print width. Use English text in the figure.

The visual style should feel like a carefully drawn scientific schematic: white background, softly rounded boxes, pale pastel fills, clear dark arrows, and friendly, slightly handwritten lettering. Use pink/magenta for vision–language features and slots, cyan/blue for visual features and slots, pale yellow for lightweight readouts, and pale green for task decoders. Keep the composition airy, refined, and visually balanced. You have creative freedom over layout, grouping, icons, and how to illustrate the mechanisms; prioritize an immediately understandable visual story over exhaustive implementation detail.

Scientific content:

The VLM provides two distinct feat ure sources: vision–language hidden states after multimodal fusion, and native vision-encoder features before language fusion. Select four depths from each source: an early layer, two intermediate layers, and the final layer. A simple stack or sequence of layers with four colored selections can communicate this hierarchy. Source-specific lightweight readouts turn these into four pink vision–language slots and four blue visual slots. The visual slots use learned-query attention pooling over visual patch tokens and never read report or fused language features. Keep this source separation visually clear, even if the readouts share an enclosing box. Together the two groups form the state S, labeled “8 slots” and “8 × d”. Slots represent different depths, without fixed organ, disease, or image-region assignments.

Task supervision shapes the slots jointly with the decoders. Classification and report generation read all eight slots. Segmentation and super-resolution read only the four blue visual slots together with the input image, which supplies spatial detail. Show these task families compactly, with an intuitive distinction between their inputs. A small “Joint optimization” annotation can convey that task gradients train the readouts. For super-resolution, both visual slots and image guidance come from the low-resolution input.

Include a clear but compact auxiliary branch highlighting the added visual constraint, labeled “Visual slot spatial consistency” and marked “Training only · optional”. The four blue visual slots alone feed a small reconstruction module that produces a spatial feature grid. A frozen vision teacher independently encodes the input image and two crops of that same image. Match the corresponding full-image and crop regions of the reconstructed feature field to these teacher features through a consistency loss. An elegant feature-grid-and-crops illustration is welcome. The reconstruction module receives no image pixels; the teacher branch provides supervision only. For super-resolution this branch also uses only the low-resolution input. Keep it visually distinct from the image-guided segmentation and super-resolution decoders.

Let the main slot-construction flow dominate, with the auxiliary consistency mechanism naturally integrated into the available space. Use short labels and meaningful visual elements rather than explanatory paragraphs. Choose the clearest arrangement yourself, minimize crossing arrows, and omit unnecessary architecture internals. Deliver the figure itself, without an external caption, figure number, or surrounding commentary.
