Create a publication-ready Figure 1 for a CVPR paper introducing MedWorld-JEPA, a predictive patient representation model. Design it as a single elegant landscape scientific illustration with a clear left-to-right story and a strong central focal point.

The story begins with multimodal patient evidence available by time t: a compact cluster of de-identified 2D and 3D medical studies together with report, history, timing, and context tokens. Images enter a shared medical 2D / 3D encoder. From the current study, the encoder retains a multiscale dense spatial memory Dₜ¹:ᴸ. For each permitted study, a resampler and projector create a small set of compact visual tokens. Report and context tokens join these projected visual tokens directly.

At the visual center, show a VLM with LoRA as a refined token sequence: visual tokens, text tokens, and then exactly eight generic patient-state slots. The returned slot states form Sₜ. Present the dense memory as a companion layer above or around the slot sequence, and gently unite Dₜ¹:ᴸ and Sₜ as the reusable current representation Rₜ = (Dₜ¹:ᴸ, Sₜ). Make this central present-state motif the largest and most memorable element in the figure.

A slim read-only query interface reads Sₜ after the state is formed. Let it open into two graceful branches. One branch represents downstream task reuse, with a compact global clinical output and a dense localization or segmentation output that can also draw on Dₜ¹:ᴸ. The second branch represents horizon-conditioned forecasting: a small horizon h cue enters the forecast query to produce Aₜ,ₕ; Dₜ¹:ᴸ, Sₜ, Aₜ,ₕ, and h then flow into a compact latent predictor.

Compose the right side as a balanced future-learning motif. In the upper right, an observed future study Xₜ⁺ passes through an EMA target encoder and pooling module, becoming a stop-gradient hierarchy of one global latent and several regional multiscale latents. In the lower right, the latent predictor produces a matching hierarchy of predicted global and regional future latents. Let the target and prediction gently curve toward one another and meet at a small latent comparison or loss symbol near the right center. A subtle returning stroke indicates that future prediction loss shapes the present representation Rₜ.

Use a central-hinge composition: the left evidence cluster feeds the central present state; the task branch lifts toward the upper right; the forecast branch flows toward the lower right; and the future target and prediction meet as a balanced pair. Use scale and spacing to make the VLM slots and Rₜ the visual hero, with the surrounding modules acting as supporting elements.

Create an elegant editorial scientific illustration in the visual spirit of a Nature main-journal methods figure, on a luminous warm-ivory field. Use refined flat vector illustration, compact medical pictograms, softly rounded abstract tokens and feature maps, delicate graphite linework, and graceful flowing connections. Use restrained mineral colors: a dominant slate-blue and mineral-teal family for current evidence and dense features, muted plum for the VLM and patient state, soft sage for the EMA target, and warm terracotta for prediction and future loss. Let neutrals and open space carry most of the composition, using color selectively to guide the eye. Give the scans, tokens, networks, and latent grids one cohesive illustration language.

Keep typography sparse and integrated beside the relevant objects. Use short labels such as “Evidence ≤ t”, “2D / 3D encoder”, “Dense memory Dₜ¹:ᴸ”, “Resampler + projector”, “VLM + LoRA”, “8 generic slots Sₜ”, “Rₜ = (Dₜ¹:ᴸ, Sₜ)”, “Read-only queries Q”, “Latent predictor”, “Future Xₜ⁺ · EMA target”, “Prediction Ŷₜ,ₕ”, and “Future loss shapes Rₜ”. Small symbols such as q, h, and Aₜ,ₕ can sit naturally on the corresponding branches.

Use your own design judgment for the exact geometry, proportions, icons, spacing, and connection paths. Prioritize beauty, compositional unity, and immediate understanding when the figure is reduced to full two-column paper width.
