Create a publication-ready Figure 1 for MedWorld-JEPA entirely from this text. Show one medical image encoder, image-text fusion in a VLM, and shared patient-state tokens feeding downstream tasks and a latent world model. The highest layout priority is compactness: keep modules close, integrate token displays into the modules they belong to, and use short local connectors.

COMPACT COMPOSITION
Use a tightly composed landscape canvas, approximately 1.9:1 in width to height, on a pure white background. Trim the outer margins to about 2-3% of the canvas. Fill the available area with readable modules and their immediate connections. Avoid empty bands above or below the architecture, large vacant rectangles between components, and a separate central column devoted to tokens. Preserve readable text sizes while reducing distances between modules.

Arrange three closely connected groups: a compact two-row evidence-and-encoding group on the left; one central VLM containing its input token strip; and two stacked readout branches on the right. The VLM sits vertically between the upper task branch and lower world-model branch. Its output state tokens occupy only a narrow gap between the VLM and the readout modules. Keep the two readout modules close together, with a gap no larger than about one quarter of a decoder's height. Place each output and loss immediately beside its producing module. Most connectors should span only a small inter-module gap.

Eliminate long spatial-feature bypass wires using shared-feature ports. At the encoder output, draw one small blue circular port labeled D_t and the short annotation "Shared image features". Draw two matching small blue D_t ports: one attached directly to the spatial input of the segmentation decoder/head, and one attached to the world-model input. These identically labeled ports refer to the SAME encoder feature tensor, not separate encoders or independent inputs. Each destination port uses a short local arrow. Do not connect these ports with full-width lines. Do not draw any gray rectangle, perimeter route, or top/bottom bypass spanning the VLM. This port notation preserves the feature connections while keeping the drawing compact.

VISUAL STYLE
Use flat colored shapes with clear dark outlines, small grayscale medical thumbnails, and sparse labels. Draw the V-JEPA encoder as a cyan-blue trapezoid tapering toward its output. Draw the VLM and readout modules as rounded rectangles. Draw task heads as slim pale-blue trapezoids with short vertical labels. Draw resampling and tokenization as small plain rectangles.

Use cyan-blue #9BCAE9 for the encoder; mauve-pink #D399D9 with a dark plum #68256C outline for the VLM; light green #B8E094 with an olive #557C2F outline for the task decoder; warm beige #D8CBBB with a brown #8C6639 outline for the world model; pale cream-yellow #FFFFCD for internal blocks; and very pale blue #DFE9F6 for heads. Draw tokens as small rounded squares. Use cyan for image tokens, neutral gray for text and future targets, and pink for state slots and patient-state tokens.

Inside the VLM and decoder containers, show only a few narrow upright pale-yellow blocks with black outlines and a small ellipsis. Short vertical labels such as "Transformer block" or "Predictor block" suggest the computation without specifying layer counts. Do not make these decorative internal blocks so large that they force extra canvas area. Use rounded, slightly handwritten sans-serif titles similar to Comic Sans, a clean narrow sans-serif for small labels, and bold italic mathematical labels for D_t and S_t. Use black arrows with small triangular heads and short right-angle turns. A tiny orange flame may mark a trainable decoder or predictor. Keep the figure flat, without shadows, gradients, legends, a title banner, or a caption inside the artwork.

LEFT GROUP: CURRENT EVIDENCE
Place two small grayscale medical thumbnails next to each other, labeled "Current / prior studies". Show them as examples of available imaging, without implying that every patient has every modality. Their image route is short and direct:
medical studies -> V-JEPA encoder -> Resampler + projector -> VLM
Place the encoder and resampler close together. Mark the encoder's single-level feature output with the D_t port. Do not draw an additional tall feature-token column here.

Immediately below this image route, place a small two-line text card labeled "Report / history / timing", followed by a compact "Tokenizer + embedding" block. Route its output directly into the VLM with a short arrow. Keep this text row close to the image row. Text must not pass through the image encoder. All online evidence is available at or before time t.

CENTRAL GROUP: VLM WITH AN INTEGRATED INPUT STRIP
Place the pink VLM immediately after the resampler and tokenizer. Put one compact input token strip INSIDE the VLM container, just below its title and above its Transformer blocks. The strip contains three adjacent groups in this exact order:
visual tokens | text tokens | learned slots
Use a few cyan glyphs for the visual group, a few gray glyphs for the text group, and eight small pink glyphs for the learned slots. Label the three groups with short text beneath the strip. Both incoming arrows enter this shared VLM input area. The token strip is part of the VLM drawing, not a separate module.

Show visual and text tokens ONLY in this integrated strip. Do not draw standalone visual-token rows after the resampler, standalone text-token rows after the tokenizer, or a second concatenated sequence floating between those modules and the VLM. The resampler and tokenizer connect directly to the VLM. This removes duplicated token illustrations and the large empty middle area they create.

Label the container "VLM" with a small "LoRA" annotation. Place eight output patient-state tokens in a narrow pink column directly against the outside of the VLM's right edge, with a very short output arrow. Label this column "S_t" and "Patient state"; avoid several stacked labels saying the same thing. These are the contextualized hidden states of the eight learned slots. They are continuous representations of the current patient, formed before any task query or forecast horizon is introduced. Both branches read this same S_t through a short branching connector; do not partition the slots into fixed task-specific roles.

UPPER RIGHT: TASK READOUT
Place a compact green "Task decoder" close to the state-token column. A single small "Task query" token sits directly beside the decoder's input edge and connects with exactly one short arrow. Do not draw a second arrow from the query label. Follow the decoder immediately with a slim "Task head" and two small outputs labeled "Clinical prediction" and "Segmentation". Use a compact score glyph and a small mask thumbnail, with a small "Task loss" label beside them. The matching D_t port connects directly to the spatial input of the segmentation decoder/head. The query readout reads S_t without writing back into it; task supervision can still train the online state-producing path.

LOWER RIGHT: WORLD MODEL AND LOCAL SUPERVISION
Place a compact beige "World model" immediately below the task decoder, with the subtitle "Latent predictor". It reads the same S_t and the matching D_t visual-feature port. Place "Forecast query" and "Horizon h" as two small local conditioning tokens immediately beside the module, each with a very short arrow. Introduce these conditions only after S_t has been formed. Keep transition-query readout and latent prediction grouped within this one module.

Immediately after the world model, draw one short column of "Predicted future tokens", a small "Prediction loss" node, and a matching short gray column of "Future target tokens". Keep this whole comparison group tightly packed; the target column must not sit at a distant canvas edge. Both token columns feed the loss. Under the target tokens, write "Real follow-up; EMA, stop-grad" in two short lines. Add the small note "Future loss shapes patient state" close to the loss. Future targets are embeddings of the actual follow-up scan and never enter the current VLM or predictor as forward inputs. Only the target is stop-gradient; the future loss trains the predictor and online state-producing modules.

FINAL CONSTRAINTS
Keep the image encoder at one feature level and retain the shared VLM, eight state slots, task branch, world-model branch, and future supervision. Do not add multiscale features, feature pyramids, separate global/regional targets, generated future scans, a second encoder, a green EMA panel, update formulas, or long feedback arrows. Do not mark online modules as frozen. Keep every tensor connection unambiguous while using the repeated D_t ports to avoid long bypass lines. The final impression should be a tightly arranged scientific schematic with one integrated VLM token strip, a narrow state-token column, and closely spaced output branches.
