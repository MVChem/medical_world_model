\section{Conclusion}

\method{} is a medical latent world model for future state prediction and reusable multimodal representation. It combines multi-depth vision--language and native visual slots, jointly trains their readouts and decoders with a horizon-conditioned dynamics predictor in a single stage, using current-task annotations and longitudinal observations at every update. Current dense-task decoders receive the input image directly and use the four visual slots as a learned condition. Current and future observations use the same encoding design, with future evidence confined to a stop-gradient target branch. \pending{insert the final future-prediction and downstream-task results}. The two evaluations assess forecasting and the current state's use across clinical and spatial tasks.
