Medical world models aim to anticipate how a patient's condition evolves. Realizing this goal requires a predictive state that connects clinical meaning with visual evidence. We introduce \method{}, a medical world model that learns the dynamics of multimodal patient states through joint-embedding prediction. Images and reports define a shared latent state space, and actual follow-ups supervise its temporal evolution. A multi-depth encoder constructs compact states from complementary vision--language and visual slots. Clinical and spatial task supervision shapes these states jointly with a horizon-conditioned world model that predicts the state encoded from a real follow-up image--report pair. Both objectives are optimized from the first update in a single training stage. This JEPA objective operates directly in the multimodal state space and updates the current-state encoder together with the dynamics predictor. Future observations enter only the target branch. The resulting model predicts latent follow-up states and conditions a shared image-token decoder for classification, visual question answering, and segmentation. Our evaluation protocol examines clinical status, progression, report fidelity, and probabilistic reliability in forecasting, alongside these three downstream tasks for current-state reuse. Clinical forecast readouts and matched downstream comparisons remain pending. \pending{add the principal forecasting and downstream results from Tables~\ref{tab:future_prediction} and~\ref{tab:downstream_tasks}}.
