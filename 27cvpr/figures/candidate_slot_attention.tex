% Use only if the implemented slot readout has genuine attention weights.
\begin{figure}[!htbp]
    \centering
    \candidatefigureplaceholder{Slot-readout attention (if available)}
    \caption{Planned patch weights from actual slot-readout attention, grouped by source and depth. This panel requires an attention-based readout; slots have no prescribed anatomical assignments.}
    \label{fig:candidate_slot_attention}
\end{figure}
