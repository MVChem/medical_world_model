\begin{figure}[tbp]
    \centering
    \candidatefigureplaceholder{Slot-conditioned segmentation and super-resolution}
    \caption{Planned segmentation and $4\times$ super-resolution outputs from image-only, frozen-slot, and jointly trained models, with matched crops and references. Human masks and pseudo-label references are identified separately.}
    \label{fig:candidate_spatial_outputs}
\end{figure}
