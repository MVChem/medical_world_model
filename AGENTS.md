# 项目开发约定

## 不保存可重算的中间缓存

用户于 2026-09-18 要求清理整个仓库的实验缓存，并改为使用时即时计算。本约定适用于
本仓库后续训练、验证、测试、可视化与代码重构。

- 不预计算并落盘保存全数据集的视觉特征、JEPA／VLM hidden states、slots、embedding、
  多视图教师特征、语义打分或其他中间激活。这些结果体积过大，不要固化为持久依赖。
- 可从原图重算的 resize、padding canvas 和 LR 输入也应在当前 batch 内即时生成。
  训练与 test time 均直接执行所需预处理和模型前向；中间张量只在本次计算所需的
  内存／显存中保留，不建立跨样本、跨运行的持久特征库。
- **旧代码报缓存文件不存在时，修改并重构代码，从原始输入即时重新计算。**
  不恢复已删除的缓存，不自动重建，不要求用户先执行缓存生成脚本，也不以
  “兼容旧实验”为理由保留磁盘缓存依赖。
- 后续涉及这些调用链时，删除旧的缓存生成／加载分支，改为调用预处理函数、编码器
  或教师模型。不要仅把写入目录移到 `/tmp`、仓库外、共享目录或其他磁盘。
- 原始数据、固定标注（含既有 CXAS 伪标签监督）、患者划分、预训练权重、训练
  checkpoint、配置、日志和最终评测结果属于数据或实验产物，不作为中间缓存删除。
  缺少这类必要输入时应明确报告，不能伪造或将其误当作可重算的特征缓存。
- 允许导出论文所需的少量选定病例预测、真实 attention 及图件；不得把可视化导出
  扩展成全数据集特征缓存。attention 必须来自实际前向过程。
- 新增数据及大体积最终实验产物的实体放在 `/home/data2/chk/data`，仓库通过 symbolic
  link 引用；该存放约定不是允许把中间缓存移到该目录继续保存。

历史代码、配置、笔记或源码快照中可能仍有缓存命令，它们仅记录旧实验，不代表当前
执行方式。后续修改以本约定及用户最新指示为准；不要为了缓存缺失而删除模型训练
逻辑、原始数据或其他无关代码。

运行检查时可使用 `PYTHONDONTWRITEBYTECODE=1 python -B`，pytest 加
`-p no:cacheprovider`，避免重新留下 `__pycache__` 和 `.pytest_cache`。
